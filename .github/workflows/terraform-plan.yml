name: Terraform (legacy)

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read

jobs:
  terraform:
    name: Terraform (legacy)
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Legacy workflow kept for compatibility. Use .github/workflows/terraform.yml for PR and push checks."
