# Plain HTTP overrides (port is defined in main httpd.conf)

# Avoid "could not reliably determine the server's fully qualified domain name" warnings
ServerName localhost
<Location /favicon.ico>
    ErrorDocument 404 "No favicon"
</Location> 
# Security: Referrer-Policy header
Header set Referrer-Policy "origin-when-cross-origin"
